package com.pillmate.notification.domain.model;

public enum NotificationType {
    DOSE_REMINDER,
    DOSE_TAKEN,
    DOSE_MISSED,
    DOSE_CANCELED,
    DOSE_OVERDUE,
    DOSE_NUDGE,
    DDI_CRITICAL,
    PRESCRIPTION_NEW,
    WEEKLY_REPORT,
    GROUP_MEMBER_JOINED
}
