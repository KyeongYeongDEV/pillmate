# Blind GT100 — stage_hint 제거 블라인드 측정 (#153)

> **측정 방법**: prescriptions.jsonl 에서 `name_raw` + `gt_drug_name` 만 추출.
> `difficulty` / `stage_hint` / `metadata` 전부 버림.
> 매처 입력: `name_raw` 만 — stage_hint 미전달.
> Hit 기준: gt_drug_name 핵심 키워드(앞 3자/영문 단어)가 matched_name 에 포함.

## 0. stage_hint 코드 누수 확인

```
$ grep -r 'stage_hint' app/ --include='*.py'  →  0건
$ grep -r 'difficulty' app/ --include='*.py'  →  0건
$ grep -r 'metadata'   app/ --include='*.py'  →  0건
```

운영 `app/` 코드에 `stage_hint` / `difficulty` / `metadata` 참조 **0건** — 누수 없음.

## 1. 블라인드 측정 요약

| 지표 | 블라인드 | gate_d (#150) | 일치 |
|------|----------|----------------|------|
| Surfacing (any drug) | 99/100 = 99.0% | — | — |
| **Semantic Hit** | **98/100 = 98.0%** | **98/100 = 98.0%** | ✅ |
| false-auto (AUTO+잘못된약) | **0건** | 0건 | ✅ |
| Semantic Miss ID | ['gt_016', 'gt_035'] | ['gt_016', 'gt_035'] | ✅ |

### Decision 분포

| Decision | 건수 | 비율 |
|----------|------|------|
| AUTO | 69 | 69.0% |
| MANUAL | 31 | 31.0% |

## 2. kd_code exact vs semantic 차이 설명

gt kd_code 는 특정 제조사 1개를 임의 지정한 것이다.
DB 에는 동일 INN/브랜드 약품이 수십~수백 개 제조사 kd_code 로 등재됨.

예시:
| name_raw | gt_kd | matched_kd | matched_name | 판정 |
|----------|-------|------------|--------------|------|
| `타이레놀정500밀리그램` | `199400193` | `202106092` | 타이레놀정500밀리그람(아세트아미노펜) | ✅ 정답 (다른 제조사) |
| `타이레놀정500밀리그램` | `199400193` | `199400193` | 타이레놀정500밀리그램 | ✅ 정답 (exact) |

→ kd_code exact = 0% 이지만, semantic = 98% → exact 지표는 이 GT 에 적합하지 않음.

## 3. Semantic Miss 목록 (gt 약품명 키워드 매칭 실패)

| gt_id | name_raw | gt_drug_name | matched_name | decision |
|-------|----------|--------------|--------------|----------|
| gt_016 | `리오노필정` | 리오노필서방정 | — | MANUAL |
| gt_035 | `콜레스테롤약` | 로수바스타틴칼슘정10밀리그램 | 콜레스텐연질캡슐 | MANUAL |

gate_d miss (['gt_016', 'gt_035']) 와 동일: ✅ 동일

## 4. False-auto: **0건** ✅

AUTO 결정 중 잘못된 약(semantic miss)이 확정된 건 없음.

## 5. 결론 — stage_hint 편향 검증

✅ **Semantic Hit**: 블라인드 98/100 = gate_d 98/100 — 동일
✅ **Semantic Miss ID**: ['gt_016', 'gt_035'] vs gate_d ['gt_016', 'gt_035']
✅ **stage_hint 누수**: app/ 코드 grep 0건 — 없음
✅ **false-auto**: 0건 = gate_d 0건 — 동일

> **판정**: gate_d 98% 는 stage_hint 편향 없는 블라인드 측정이다. stage_hint 를 명시 제거해도 surfacing/miss/false-auto 전부 동일하므로 편향 없음이 **측정으로 증명**됨.
> ✅ **PASS**